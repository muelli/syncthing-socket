# Compiled binary
/syncthing-socket

# TLS certificates
cert.pem
key.pem

# Local Go installation
local/

# Test binaries
# Anchored to the repository root: these are the e2e suite's build artefacts
# (test-server, test-auth-binary and friends). Unanchored, this also swallowed
# contrib/test-vm/ and anything else named test-* anywhere in the tree.
/test-*
/dist/
.venv/

# Antigravity multi-agent workspace
.agents/

# Android build outputs and the gomobile AAR, all regenerated by the build
android/.gradle/
android/app/build/
android/app/libs/
